import './globals.css';
export const metadata={title:'TextGuard — Write with confidence',description:'Humanize, paraphrase and run strict plagiarism checks.'};
export default function Layout({children}){return <html lang="en"><body>{children}</body></html>}
